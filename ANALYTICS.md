# 飞跃手册：数据统计接入与运营口径

本项目继续使用 MkDocs Material + GitHub Pages。前台访问不要求注册；统计服务独立部署，管理人员通过统计服务自己的登录入口查看数据。

## 交付状态

- 已完成可选 Umami 接入、事件采集、搜索参数清理、二维码展开交互。
- 未配置服务时，不加载统计脚本、不发送事件、不显示虚构数据。
- 统计服务器、数据库、管理员账号尚未创建；现有站点的历史浏览量未取得。
- 定制后台的界面为设计提案。第一阶段推荐先使用 Umami 自带看板，确认指标有用后再开发统一后台。

## 部署选择

优先复用已有服务器运行 Umami + PostgreSQL；没有服务器时，可评估其托管服务。自托管软件开源，但服务器、数据库、备份和维护仍有成本。面向国内用户，上线前应从真实用户网络验证脚本与采集请求的可达性。

参考 [Umami 官方安装文档](https://docs.umami.is/docs/install)。部署时选择并固定经过验证的版本，配置 HTTPS、数据库持久化与备份，使用独立管理员账号。后台的身份验证在服务端完成；不能用静态页面中的密码判断保护数据，也不能向公开前端写入数据库密码、管理员令牌或 GitHub 凭据。

## 配置网站

在已部署的 Umami 中添加站点，获得跟踪脚本地址和网站 UUID。在 GitHub 仓库的 Actions Variables 中配置：

| 变量 | 内容 | 是否属于秘密 |
| --- | --- | --- |
| FEIYUE_ANALYTICS_SCRIPT | Umami 提供的 HTTPS 脚本地址 | 否 |
| FEIYUE_ANALYTICS_WEBSITE_ID | 该站点 UUID | 否 |
| FEIYUE_ANALYTICS_DASHBOARD | 管理员登录入口，可留空 | 否 |

前两项必须同时提供或同时留空。后台入口不得填写带共享令牌或查询参数的公开分享链接。第三项配置后，页脚会显示“管理统计”，真正的登录由 Umami 负责。

构建使用这些变量渲染公共配置。网站 UUID 是采集标识，不是访问统计数据的凭据。脚本仅在 `www.zmzfeiyue.com` 和 `zmzfeiyue.com` 加载采集逻辑；本地与开发预览不应进入正式统计。

[官方采集接入说明](https://docs.umami.is/docs/collect-data)；[跟踪配置说明](https://docs.umami.is/docs/tracker-configuration)。

## 第一版看板

| 模块 | 主要指标 | 用途 |
| --- | --- | --- |
| 访问概览 | 页面浏览量、估算访客、访问趋势、设备类型 | 判断有多少人使用、增长来自哪里 |
| 内容表现 | 案例页面浏览量、院校阅读排名、有效阅读事件 | 判断大家对哪些经历感兴趣 |
| 发现内容 | 搜索次数、无结果比例、筛选使用 | 发现内容缺口与查找困难 |
| 宣传渠道 | UTM 渠道的访问量、案例阅读、问卷点击 | 比较公众号、微信群、海报带来的效果 |
| 参与行为 | 问卷点击、群二维码展开 | 衡量继续了解或参与的意愿 |
| 内容供给 | 案例总数、院校数、届数、缺失字段 | 指导征稿与资料核对 |

内容供给来自案例元数据；其他指标必须来自采集服务。不要把案例数量、GitHub 仓库访问量或 CDN 请求数当成网站访客数。

## 事件定义

页面浏览由 Umami 自动采集。下列是额外行为事件；看板中 PV 必须只聚合页面浏览事件，不能把按钮点击混入。网站当前没有启用 Material instant navigation；后续启用时应重新验证导航与事件监听不会重复。

| 事件 | 触发时机 | 允许的附加数据 |
| --- | --- | --- |
| case_card_click | 点击案例卡片 | case_id，基于文件路径哈希 |
| case_search | 提交搜索，或输入停顿后 | 结果数、有无关键词、届数、科类；不含原始输入 |
| case_filter | 改变届数/科类 | 同上 |
| case_engaged | 页面可见累计至少 30 秒，且读到文章区域至少 50% | case_id、可见秒数、阅读位置百分比 |
| contribute_start | 从首页进入投稿指南 | 无 |
| questionnaire_click | 点击跳转问卷星 | 无 |
| community_open | 从案例详情进入交流群页面 | 无 |
| qr_expand | 每次页面加载后首次展开对应群二维码 | qq 或 wechat |

`case_engaged` 是阅读投入的代理指标，不代表理解或读完；后台的“有效阅读率”应在同一统计周期、同一案例下，以发生该事件的访问去重后，除以打开该案例的访问去重数。不要直接拿事件次数除以访客人数。

问卷点击不等于投稿成功。问卷星完成数要通过获授权的问卷导出或接口补充；二维码展开不等于扫码或入群。现有技术无法直接确认微信/QQ 实际入群。

UV 是工具依据匿名技术标识估算的访客数，不是精确人数；每日 UV 相加也不是整月去重 UV。保留提供者自己的去重口径，并在跨月、跨设备等场景中说明局限。见 [Umami 指标定义](https://docs.umami.is/docs/metric-definitions)。

## 宣传归因与采集边界

- 公众号、微信群和海报使用不同 UTM 标记，例如 `utm_source=wechat_group&utm_medium=social&utm_campaign=anniversary90`。普通微信分享不保证提供可靠来源；缺失标记的访问保留为直接/未知。
- 每个 UTM 值使用简短 ASCII 活动代码，不能放姓名、电话或个人标识。
- `analytics-sanitize.js` 仅保留指定 UTM 字段，移除 `q`、搜索高亮参数及 hash；外部来源仅保留 origin。
- 尊重浏览器 Do Not Track。第一版不启用用户身份绑定、会话录像或表单文本采集。
- 测试人员可使用 Umami 官方的排除自身访问功能；统计脚本被浏览器拦截、禁用 JS 等会产生漏计，应按工具口径解释。
- 原始事件保留周期、备份周期由部署者明确设置，例如短期明细与长期汇总分开保留。不要承诺恢复未采集的历史数据。

## 上线验收

1. 在开发预览验证首页、案例筛选、详情、投稿入口与二维码，包含移动端。
2. 配置真实统计服务后，用正式域名检查脚本及采集请求成功，后台出现一条对应页面浏览。
3. 排除测试流量，再分别触发搜索、问卷点击和二维码展开，检查事件名与参数。
4. 后台与 API 必须在未登录时拒绝读取私有统计；不要启用公开分享链接来冒充登录保护。
5. 核对北京时间报表、PV 事件类型、访客口径，以及 www / 裸域名归一；确认旧链接仍可访问。

未来如需定制“飞跃手册数据后台”，由服务端保存统计 API 凭据并代理聚合查询；前端只读取经登录鉴权的汇总接口。内容审核仍可先走 `dev → PR → main`，后续再增加草稿、审核、发布记录等功能。
