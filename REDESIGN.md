# 飞跃手册 UI 改版说明

基线：main / dev 的 `f0b97aaf67817cd1c9e4d52ab9f8eba5281fe06d`，2026-07-26。

## 已实现

- 首页采用温暖白底、深绿色标题与独立校园照片；将搜索、案例入口、111 份案例和 79 所院校等自动计算的数据前移。
- 精选故事由 `data/featured.yml` 明确选择；不再把按文章长度排列的内容标为“最新”。学校卡片从首页 20 张缩减为 8 张，其余保留完整入口。
- 案例库直接展示完整案例集合，支持关键词、毕业届数和科类组合筛选、结果数、空状态、重置与分批展示；首页搜索直接进入该页面。禁用 JavaScript 后仍能阅读静态案例列表。
- 案例详情的标题由构建钩子自动改为学校＋专业，改善检索辨识度；移除空导航列，压缩基本信息区，增加返回与继续阅读入口。
- 缺失成绩、位次、届数显示为“未提供”，不再呈现 0 分/第 n 名/0 届；未补造任何校友资料。有效届数统计排除未知届数。
- 阅读正文和原案例路径保留。修正一个原有失效的内蒙古大学案例链接，保留该旧页面并引导前往案例库。
- 统一卡片、边距、按钮与移动布局，使用系统中文字体，避免依赖 Google Fonts 请求。
- `site_url` 使用已验证可访问的 `https://www.zmzfeiyue.com/`；DNS、裸域名重定向仍应由域名管理者在发布前确认。
- 生成器加入失败退出与重复运行稳定性；CI 从数据重新生成卡片，避免手改生成页面被后续覆盖。构建模板不再复制为公开 HTML。

## 维护方式

安装 `requirements.txt`，运行 `python scripts/generate.py`，再运行 `python -m mkdocs serve`。完整验证使用 `python -m mkdocs build --strict`。

源头是 `data/cases.yml`、`data/featured.yml`、案例 Markdown、生成器和模板。标有 `AUTO-GEN` 的卡片区域不应手工修改。案例元数据与正文同步仍按仓库原有 `sync.py` 流程维护。

## 后台范围

本改版已实现统计接入层，未运行统计服务器。推荐第一版用 Umami 自带登录与数据看板；定制看板仍属于设计提案。部署、事件口径与验收见 [ANALYTICS.md](ANALYTICS.md)。

## 检查与剩余事项

已完成默认关闭统计和配置示例统计两种构建，以及无头 DOM 交互检查。当前环境的预览浏览器阻止打开本地页面，尚未完成真实浏览器的新 UI 视觉验收；需在发布前检查电脑与手机布局、Material 全站搜索、群二维码和浏览器返回行为。

未向远程仓库推送，未部署到正式域名。推送继续遵循仓库已有开发分支和 PR 约定。
